import { BarcodeFilterPipe } from './barcode-filter.pipe';

describe('BarcodeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BarcodeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
