import { SneakerFilterPipe } from './sneaker-filter.pipe';

describe('SneakerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SneakerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
